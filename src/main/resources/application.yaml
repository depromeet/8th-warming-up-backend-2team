spring:
  jpa:
    hibernate:
      ddl-auto: none
    database: mysql
    open-in-view: false
    show-sql: true
  datasource:
    hikari:
      maximum-pool-size: 100
      minimum-idle: 10
      idle-timeout: 30000
      connection-test-query: "SELECT 1"